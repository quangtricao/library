import { Box, Typography } from '@mui/material';

const Introduction = () => {
  return (
    <Box>
        <Typography sx={{ fontSize: '30px', fontWeight: 'bold' }}>Welcome to the Library</Typography>
    </Box>
  );
};

export default Introduction;
